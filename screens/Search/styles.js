import { Constants } from "expo";

export default {
    header: {
        backgroundColor: '#fff'
    },
    container: {
        paddingTop: Constants.statusBarHeight,
        marginBottom: 5
    },
}